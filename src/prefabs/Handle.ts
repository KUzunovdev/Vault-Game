import { Sprite, Texture } from "pixi.js";

export default class Handle extends Sprite {
  constructor(texture: Texture) {
    super(texture);

    //add logic for rotating how and when also player interaction
  }
}
